using TMPro;
using UnityEngine;

public class DummyPlayersOnline : MonoBehaviour
{
    [SerializeField] private TextMeshProUGUI _playersOnlineText;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //PhotonNetwork.
    }
}
